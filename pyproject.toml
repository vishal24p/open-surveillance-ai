[project]
name = "open-surveillance-ai"
version = "0.1.0"
description = "Student-sized local-first restricted-zone surveillance assistant"
requires-python = ">=3.11,<3.12"
dependencies = [
    "opencv-python>=4.10,<5",
]

[tool.uv]
package = false
